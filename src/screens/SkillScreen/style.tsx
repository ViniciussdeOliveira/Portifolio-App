import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#001746dc',
        alignItems: 'center',
        justifyContent: 'center',
        paddingBottom: 100,
        paddingTop: 70,
    },
});

export default styles;